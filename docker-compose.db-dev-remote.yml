# 108_xiaoniaoyun_db_dev ¡¤ MM-V3-CORE-01 (192.168.1.223:3320)
name: 108_xiaoniaoyun_db_dev

services:
  db:
    image: mysql:8.0
    container_name: 108_xiaoniaoyun-mysql-dev
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE:      ${DB_NAME:-xiaoniao}
      TZ:                    Asia/Shanghai
    ports:
      - "3320:3306"
    volumes:
      - db-data:/var/lib/mysql
      - ./docker/mysql/my.cnf:/etc/mysql/conf.d/custom.cnf:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 40s
    networks:
      - db

networks:
  db:
    name: 108_xiaoniaoyun-db-dev-net
    driver: bridge

volumes:
  db-data:
    name: xiaoniao-prod-db-data
