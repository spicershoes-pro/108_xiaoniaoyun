# 用户端 · staging 环境
# 使用方式：cp env/.env.staging .env.staging

VITE_APP_ENV=staging
VITE_APP_NAME=霄鸟云
VITE_APP_VERSION=1.0.0

# API 地址（开发由 vite proxy 转发；生产填写真实地址）
VITE_API_BASE_URL=/api

# 前端端口（参考，实际由 vite.config.js 控制）
# PORT=5173
